#!/bin/bash

# Get the C file name from the $CFILE variable
c_file="$CFILE"

# Remove the extension from the C file name
c_file_name="${c_file%.*}"

# Set the output file name with .o extension
output_file="$c_file_name.o"

# Compile the C file without linking
gcc -c "$c_file" -o "$output_file"
